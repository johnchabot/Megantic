The Core XML Elements Inventory

<defs>
<path id="orange-loop">: The single geometric blueprint for your rigid enclosures. It draws a 4-column wide straight block that curls inward past the baseline to form the top and bottom caps.
<path id="blue-swoosh">: The singular mathematical master key for your double helix. A continuous cubic curve that anchors on the Row 1, Row 6.5, and Row 12 alignment ticks.
<g id="chassis-backing-panel">: The unified backing chassis block containing 1 background backing rectangle (<rect>) and 23 grid lines (<line>) that map your 10x12 guide ticks.
<g id="mechanical-slice-mask">: The physical stencil cutter array consisting of 14 flat mask tracks (<line>) to segment your loops into crisp addressable bricks.

Display Viewports (<g transform="...">)
Panel 1 Container (Left Mesh): Uses <use> tags to call your backing panel, registers the unlit deep charcoal helix tracks, layers the unlit loop tracks directly on top to enforce your priority rule, and applies the cutter mask.
Panel 2 Container (Right Print): Uses <use> tags to call the backing panel, ignites the high-intensity glowing amber double-helix tracks, draws your dominant active orange loops directly over them to cleanly swallow the overlaps, and overlays the slice grid.

